import { build } from 'esbuild';
import path from 'node:path';
import process from 'node:process';

const ROOT = process.cwd();
const ENTRY = path.join(ROOT, 'src', 'classic2-standalone.tsx');
const OUTFILE = path.join(ROOT, 'js', 'classic2-trainer.bundle.js');

async function run() {
  await build({
    entryPoints: [ENTRY],
    outfile: OUTFILE,
    bundle: true,
    format: 'iife',
    platform: 'browser',
    target: ['es2020'],
    sourcemap: false,
    minify: true,
    jsx: 'automatic',
    loader: {
      '.tsx': 'tsx',
      '.ts': 'ts'
    },
    banner: {
      js: '/* Auto-generated by scripts/build-classic2.mjs. Do not edit directly. */'
    }
  });
  console.log(`Built Classic2 standalone bundle -> ${path.relative(ROOT, OUTFILE)}`);
}

run().catch((error) => {
  console.error('build-classic2 failed:', error.message);
  process.exit(1);
});
