import { build } from 'esbuild';
import path from 'node:path';
import process from 'node:process';

const ROOT = process.cwd();
const ENTRY = path.join(ROOT, 'src', 'breen-table-lab-standalone.tsx');
const OUTFILE = path.join(ROOT, 'js', 'breen-table-lab.bundle.js');

async function run() {
  await build({
    entryPoints: [ENTRY],
    outfile: OUTFILE,
    bundle: true,
    format: 'iife',
    platform: 'browser',
    target: ['es2020'],
    sourcemap: false,
    minify: true,
    jsx: 'automatic',
    loader: {
      '.tsx': 'tsx',
      '.ts': 'ts'
    },
    banner: {
      js: '/* Auto-generated by scripts/build-breen-table-lab.mjs. Do not edit directly. */'
    }
  });
  console.log(`Built Breen Table Lab standalone bundle -> ${path.relative(ROOT, OUTFILE)}`);
}

run().catch((error) => {
  console.error('build-breen-table-lab failed:', error.message);
  process.exit(1);
});
